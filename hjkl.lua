physics.start()
physics.engine = require("physics.engine")

index.html = require("index.html")
physics.setGravity(0, 10)
update = require("update")
speed2 = require("physics.speed")
speed = require("speed")
speedx = require("physics.speed")
speedy = require("physics.speed2")
fgh = -1 * 0.2
function speedy.physics()
gravity.self.velocityd = {x = 0, y = fgh}.setGravity(0, fgh)
velocityd = velocity.update + fgh
 end

 gravity = require("physics.setgravity")
physics.gravity:connect(engine)
physics.gravity:connect(personagem)

hjkl.lua = connect("index.html")